import { Component } from '@angular/core';

/**
 * Phase 3 placeholder — will become the mobile remote control UI.
 */
@Component({
  selector: 'app-mobile-controller',
  standalone: true,
  template: `
    <div style="color:#fff;text-align:center;padding:4rem;font-family:sans-serif">
      <h2>Mobile controller — Phase 3</h2>
      <p>The mobile remote UI will be ported here in Phase 3.</p>
    </div>
  `
})
export class MobileControllerComponent {}
